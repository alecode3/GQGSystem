# =============================================================================
# GQG System — Variables de entorno (Supabase)
# =============================================================================
# 1. Copiá este archivo como .env en la raíz del proyecto:
#      cp .env.example .env
# 2. Completá los valores desde Supabase → Project Settings → API
# 3. Reiniciá el servidor: npm run dev
#
# IMPORTANTE: Usá solo la clave "anon" / "public". Nunca la service_role en el frontend.
# =============================================================================

VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.TU_ANON_KEY_AQUI
